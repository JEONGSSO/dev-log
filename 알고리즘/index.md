객체는 빠른접근
문제를 내방식대로 이해하기

```js
countChar("aaaa"); // {a:4}
countChar("Hi hello"); // {h:2, i:1, e:1, l:2, o:1}
countChar("My phone number is 12345!");

// 일단 함수를 정의하고
const countChar = () => {
  // 멈추어 시나리오를 작성
  // 대소문자를 구분하는지? 공백을 포함하는지? 특수문자를 포함하는지?
  // 시나리오를 작성하고 나서 해결 할 수 있도록 해보자
};
```
